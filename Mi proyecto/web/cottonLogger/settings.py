from datetime import timedelta

#### ---------- SETTINGS ---------- ####
SHORT_HASH_LEN = 16

DEFAULT_TOKEN_EXPIRATION_TIME = timedelta(days=30) # for token creation and authentication

TOKEN_CREATION_DELAY_TIME = timedelta(minutes=15) # for token creation
#### ------------------------------ ####